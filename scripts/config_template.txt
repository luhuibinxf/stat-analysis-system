# 配置文件模板
# 使用说明：
# 1. 运行 保存配置.ps1 来保存数据库连接信息
# 2. 运行 修改用户密码.ps1 来修改用户密码

# 数据库配置
Server=localhost
Database=WiNEX_PACS

# 认证方式
# UseWindowsAuth=true  (集成Windows认证)
# UseWindowsAuth=false (SQL Server认证，需要设置Username和Password)

# SQL Server认证示例
# Username=sa
# Password=your_password_here